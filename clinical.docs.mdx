---
description: 'A clinical app.'
labels: ['app', 'react']
---

A React application for clinical. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use clinical 
```

To run the app locally, run the following:

```
bit run clinical 
```
